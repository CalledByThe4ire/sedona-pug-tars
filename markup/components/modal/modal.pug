mixin modal(data, key)
	+b('div').modal(class=`--${key}`)&attributes(attributes)
		- const modalData = data[key];
		- const { heading, text, label } = modalData;

		+e('h2').heading= heading
		+e('p').text= text
		+e('button').btn.btn= label

